name: edu_app
description: A minimal cross-platform Flutter app (Hello World) with light/dark theme support.
publish_to: "none"
version: 0.0.1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  audioplayers: ^6.5.1
  confetti: ^0.8.0
  flutter:
    sdk: flutter
  http: ^0.13.6
  video_player: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
